# 解説

これは、数字無しで数字を表現してやろうという試みでもあったりします。

```python
-~(-~(()==()))
```

空のtuple同士を比較してBoolが作られます。

```python
>>> print(()==())
True
```

`True`は数値の1としても扱えるのでビット演算を使えます。

```python
>>> print(-~(-~(1)))
3
```

というわけで、答えは数値の`3`でした。
