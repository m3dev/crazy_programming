# map block

Author: [@vaaaaanquish](https://github.com/vaaaaanquish)

アロー関数でオブジェクトを返しているように見えます。何が出力されるでしょう。

```javascript
[1, 2, 3].map(n => {num: n}).join()[1]
```

## 実行

```sh
nvm use
node -e 'console.log([1, 2, 3].map(n => {num: n}).join()[1])'
```

## 解説

`explanation.md`で解説しています
