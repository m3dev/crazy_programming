# data object `・^・`

Author: [@vaaaaanquish](https://github.com/vaaaaanquish)

全部が顔文字 `・^・` に見えるKotlinのクイズです。実行すると何が出力されるでしょう？

```kotlin
data object `・^・`;{`・^・`:`・^・`->(`・^・`)}(`・^・`)
```

## 実行

```sh
kotlinc main.kt -include-runtime -d main.jar
java -jar main.jar
```

[Kotlin Playground](https://play.kotlinlang.org/) でもお楽しみいただけます。

## 解説

`explanation.md`で解説しています
